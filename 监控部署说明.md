# 基金「可买」监控 — 部署说明

每 6 小时自动检查监控名单里的基金是否可申购，一旦可买就发邮件到你的邮箱。
名单在网页「🔔 监控买入」里管理，自动同步到本仓库 `watchlist.json`，云端 GitHub Actions 读取并监控。

---

## 一次性部署（约 5 分钟）

### 1. 建仓库并推送代码

在 GitHub 新建一个**公开**仓库（例如 `fund-monitor`），然后把本目录推上去：

```bash
cd /Users/yaoxiaogang/funds-web
git init
git add -A
git commit -m "feat: 基金监控 + 网页版"
git branch -M main
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

> 推送需要 GitHub 登录。若未配置，可在对话框输入 `!gh auth login` 交互登录，或用账号 + Personal Access Token 作为密码推送。

### 2. 开启 GitHub Pages（让网页随时随地可访问）

仓库 **Settings → Pages → Build and deployment → Source 选 Deploy from a branch → 选 `main` / `/ (root)` → Save**。
等一两分钟后，访问地址为：`https://<你的用户名>.github.io/<仓库名>/`

### 3. 配置邮箱密钥（发提醒邮件用）

仓库 **Settings → Secrets and variables → Actions → New repository secret**，添加：

| 名称 | 值 | 说明 |
|------|----|------|
| `MAIL_USER` | `yao.xiaogang@trs.com.cn` | 发件邮箱（= 收件邮箱）|
| `MAIL_PASS` | 你的邮箱密码 / 客户端授权码 | 网易企业邮箱密码，**仅你自己填，存 Secrets 不外泄** |
| `MAIL_TO` | `yao.xiaogang@trs.com.cn` | 收件邮箱（可省略，默认同 MAIL_USER）|

> 默认走网易企业邮箱 `smtp.qiye.163.com:465`。若公司邮箱要求「客户端授权码」而非登录密码，请在邮箱设置里开启 SMTP 并生成授权码填到 `MAIL_PASS`。
> 如需改 SMTP 服务器，可在 **Variables** 里加 `SMTP_HOST` / `SMTP_PORT`。

### 4. 生成网页同步用的 Token

网页要把监控名单写回仓库的 `watchlist.json`，需要一个细粒度 Token：

1. GitHub 头像 → **Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token**；
2. **Repository access** 选 Only select repositories → 选你这个仓库；
3. **Permissions → Repository permissions** 设置两项：
   - **Contents** → **Read and write**（同步监控名单 watchlist.json）
   - **Actions** → **Read and write**（新增监控时即时触发云端发邮件）
4. 生成后复制 `github_pat_...`。

然后打开网页 → 「🔔 监控买入」→ 展开「☁ 云端同步设置」→ 填 GitHub 用户名、仓库名、Token → 保存。

> Token 只存在你浏览器本地，不写入代码、不上传仓库。建议只在自己信任的设备填写。

---

## 日常使用

- 打开网页 → 「🔔 监控买入」→ 输入买不了的基金 6 位代码 → 「添加监控」；
- 列表会显示每只基金的实时申购状态（✓ 可买 / ✕ 不可买）；
- 名单自动同步到云端；云端每 6 小时检查一次，**某只从「不可买」变「可买」时发邮件**给你（不会重复骚扰）。

## 验证是否正常

- 在仓库 **Actions** 页签 → 选「基金可买监控」→ **Run workflow** 手动触发一次，看日志是否跑通；
- 想测邮件：把一只**当前可买**的基金加入监控，并在 `monitor/state.json` 里临时把它标成不可买（或先不放它、下次再放），制造一次「不可买→可买」跳变即可收到邮件。

## 判定规则

申购状态（来自东方财富 `SGZT` 字段）只要**不含**「暂停 / 封闭 / 停止 / 未开放 / 终止」就算「能买」——即开放申购、限大额都会提醒（符合「只要能买就提醒」）。
